# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# Runtime data
*.db
*.db-journal
*.sqlite

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Bun
.bun/

# Local scratch (assistant-generated; not part of the project)
TODOS.md

# Claude Code local session artifacts
.claude/scheduled_tasks.*
.claude/settings.local.json

# Local service data (Postgres/Redis bind mounts)
/data/

# CodeRabbit CLI review output (bin/cr.ts)
.cr/

# Local asset storage (filesystem provider). STORAGE_ROOT resolves from the cwd,
# so cover both repo-root runs and starting the API from apps/api/. Anchored on
# purpose — a bare `storage/` would also ignore the `packages/storage` source.
/storage/
/apps/api/storage/
